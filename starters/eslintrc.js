const hxmstyle = require('hxmstyle');

module.exports = hxmstyle({
    // Custom options passed in this way will be safely deep-merged with the default config.
    // This is the recommended method of adding things to the config.
/*
    plugins: [],
    rules: {
    },
*/
});
